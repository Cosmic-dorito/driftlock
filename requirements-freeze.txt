# Complete `pip freeze` of the development environment, as the problem statement requires.
#
# This is the EXHAUSTIVE list: every package present while the reported results were produced,
# including test and build tooling (pytest, ruff, python-pptx, pymupdf) that the localizer itself
# never imports.
#
# To REPRODUCE THE RESULTS, install requirements.txt instead. That file is the curated runtime set
# — eight direct dependencies plus four transitive ones, each pinned — and is what README.md tells a
# reviewer to use. Installing this file also works, but pulls in tooling irrelevant to inference.
#
# Regenerate with:  python -m pip freeze > requirements-freeze.txt
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
defusedxml==0.7.1
fonttools==4.63.0
ImageIO==2.37.4
iniconfig==2.3.0
kiwisolver==1.5.0
lazy-loader==0.5
lxml==6.1.1
matplotlib==3.11.1
networkx==3.6.1
numpy==2.5.2
opencv-python-headless==5.0.0.93
packaging==26.3
pandas==3.0.5
pillow==12.3.0
pluggy==1.6.0
Pygments==2.20.0
pymupdf==1.28.2
pyparsing==3.3.2
pypdf==6.16.0
pypdfium2==5.12.1
pytest==9.1.1
python-dateutil==2.9.0.post0
python-pptx==1.0.2
PyYAML==6.0.3
ruff==0.16.2
scikit-image==0.26.0
scipy==1.18.0
six==1.17.0
tifffile==2026.7.31
typing_extensions==4.16.0
tzdata==2026.3
xlsxwriter==3.2.9
